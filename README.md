# ocr-service

[RU 🇷🇺](README.md) | [EN 🇬🇧](README.en.md)

FastAPI сервис для извлечения текста из изображений с помощью Pytesseract. В проекте RabbitMQ выступает брокером Celery, а Redis используется как бэкенд Celery и кэш.

## Быстрый старт

1. **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/EXsiDe4299/ocr-service.git
    ```
   
2. **Перейдите в директорию проекта:**
    ```bash
    cd ocr-service
    ```
   
3. **Настройте окружение:**

    Создайте файл `.env` и настройте переменные окружения.

    - Linux:
    ```bash
    cp .env.template .env
    ```
   
    - Windows:
    ```bash
    copy .env.template .env
    ```
   
4. **Запустите Docker Compose:**
    ```bash
    docker compose up -d
    ```

5. **Попробуйте приложение:**

    Откройте http://0.0.0.0:8000/docs для просмотра документации Swagger. RabbitMQ Management будет доступен по адресу http://0.0.0.0:15672

## Функционал

- **OCR** – Извлечение текста из изображений с помощью Pytesseract.
- **Фоновые задачи** – Обработка изображений в Celery с брокером RabbitMQ и бэкендом Redis.
- **Кэширование** – Предотвращение повторной обработки одних и тех же изображений с помощью Redis.
- **Управление статусами задач** – Пометка начатых задач статусом ACCEPTED для отслеживания их состояния.